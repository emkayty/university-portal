"""
offline URL Configuration
=====================
"""
from django.urls import path
# Add app-specific URL patterns here
app_name = 'offline'

urlpatterns = [
    # Add paths: path('view/', view, name='view'),
]
