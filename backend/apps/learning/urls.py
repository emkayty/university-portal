"""
learning URL Configuration
=====================
"""
from django.urls import path
# Add app-specific URL patterns here
app_name = 'learning'

urlpatterns = [
    # Add paths: path('view/', view, name='view'),
]
